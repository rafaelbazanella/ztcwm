---
phase: 20-shell-users-page-regression-fixes
plan: 04
type: execute
wave: 3
depends_on:
  - 20-01
  - 20-03
files_modified:
  - src/pages/dashboard.ts
  - src/pages/networks.ts
  - src/pages/network-detail.ts
  - src/pages/members.ts
  - src/pages/controllers.ts
  - src/pages/settings.ts
  - src/pages/logs.ts
  - src/pages/api-explorer.ts
  - src/pages/pending.ts
  - src/pages/users.ts
  - src/pages/users.test.ts
  - src/styles/theme.ts
autonomous: true
requirements:
  - LAYOUT-01
tags:
  - cleanup
  - dead-code-removal
  - pages
  - test-update
  - css-token-cleanup

must_haves:
  truths:
    - "All 13 per-page <zt-navbar ...></zt-navbar> invocations are deleted from 10 page files (D-13, D-16)"
    - "All 10 dead `import '../components/navbar.js';` lines are deleted from page files (D-13)"
    - "The page-level layout wrapper `<div style=\"margin-top: var(--space-lg, 1.5rem);\">` (or equivalent in each page) is PRESERVED to keep visual rhythm below the now-app-level navbar (Pattern D guidance from 20-PATTERNS.md)"
    - "The `it('renders navbar with User Management title', ...)` block at users.test.ts:193-199 is DELETED — the equivalent assertion now lives in app.test.ts (D-15)"
    - "No other pages/*.test.ts file contains a `zt-navbar` reference (verified by grep)"
    - "The dead `--navbar-height: 56px` token at src/styles/theme.ts:67 is removed (UI-SPEC § Navbar height token cleanup)"
    - "The `<zt-navbar>` component file itself (src/components/navbar.ts) and its component-level tests (src/components/navbar.test.ts) are KEPT — only per-page usage is removed (D-14)"
  artifacts:
    - path: "all 10 page files listed in files_modified"
      provides: "Pages with NO <zt-navbar> markup and NO navbar import"
      contains: "(verified by grep that the token does NOT contain '<zt-navbar' or 'components/navbar.js')"
    - path: "src/pages/users.test.ts"
      provides: "Users-page test file with the D-15 navbar assertion removed"
      contains: "(verified by grep that 'zt-navbar' no longer appears)"
    - path: "src/styles/theme.ts"
      provides: "Theme tokens without the dead --navbar-height declaration"
      contains: "(verified by grep that '--navbar-height' returns 0 matches)"
  key_links:
    - from: "Per-page render templates"
      to: "<zt-app> shell-level <zt-navbar> (from Plan 20-03)"
      via: "Pages no longer render the navbar; the shell renders it once"
      pattern: "(absence of <zt-navbar)"
---

<objective>
Delete every per-page `<zt-navbar>` invocation (13 instances across 10 files) and every dead `import '../components/navbar.js';` line (10 instances) now that `<zt-app>` mounts the navbar persistently (Plan 20-03 ships this). Also delete the existing users.test.ts navbar assertion (replaced by the new `app.test.ts` block from Plan 20-03 Task 3) and remove the now-dead `--navbar-height: 56px` CSS token from `src/styles/theme.ts:67`.

This plan closes D-13 (per-page deletion), D-15 (test cleanup), D-16 (uniform double-render handling), and finishes the UI-SPEC § Navbar height token cleanup.

The plan is mechanical: there are no design decisions — every change is dictated by literal source lines verified via grep in 20-PATTERNS.md. The `<zt-navbar>` component file (`src/components/navbar.ts`) and its tests (`src/components/navbar.test.ts`) are KEPT (D-14) — only per-page usage and the dead token are removed.

**Dependency note:** This plan modifies `src/pages/users.test.ts`, which Plan 20-01 also modifies (Plan 20-01 ADDS the D-03 regression test there; this plan REMOVES the D-15 navbar assertion). The two edits target different line ranges, but to avoid any chance of a stale-line-offset conflict during execution, this plan declares `depends_on: [20-01, 20-03]` so the executor guarantees Plan 20-01's edit lands first.

Purpose: Finalize LAYOUT-01 by removing the duplicate per-page renders, eliminate dead imports, and clean up the orphaned CSS token. SC #5 navbar test coverage was moved to `app.test.ts` in Plan 20-03; this plan removes the now-stale Users-page assertion.

Output:
- 10 page files in `src/pages/` with `<zt-navbar>` markup and navbar import deleted
- `src/pages/users.test.ts` — the it() block "renders navbar with User Management title" deleted (lines 193-199 pre-Plan-20-01; line offsets may differ after Plan 20-01's insertion — see Task 2 for tolerant matching)
- `src/styles/theme.ts` — line 67 (`--navbar-height: 56px;`) deleted
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-shell-users-page-regression-fixes/20-CONTEXT.md
@.planning/phases/20-shell-users-page-regression-fixes/20-PATTERNS.md
@.planning/phases/20-shell-users-page-regression-fixes/20-UI-SPEC.md
@.planning/phases/20-shell-users-page-regression-fixes/20-01-SUMMARY.md
@.planning/phases/20-shell-users-page-regression-fixes/20-03-SUMMARY.md

<interfaces>
<!-- All targets are deletions. Each is verified by literal grep output from 20-PATTERNS.md Pattern D + Pattern N theme.ts cleanup section. -->

The 13 `<zt-navbar>` invocations to delete (file:line:literal source — line numbers pre-Plan-20-04; should be unchanged by 20-01/20-02/20-03 since they don't touch page files):
```
src/pages/users.ts:523:            <zt-navbar title="User Management" subtitle="Manage accounts and roles"></zt-navbar>
src/pages/settings.ts:95:            <zt-navbar title="Preferences" subtitle="User preferences"></zt-navbar>
src/pages/members.ts:103:            <zt-navbar title="Members" subtitle="All network members"></zt-navbar>
src/pages/api-explorer.ts:230:            <zt-navbar title="API Explorer" subtitle="Test ZeroTier API endpoints"></zt-navbar>
src/pages/logs.ts:89:            <zt-navbar title="Logs" subtitle="Application event log"></zt-navbar>
src/pages/pending.ts:199:            <zt-navbar title="Pending Authorization" subtitle="Unauthorized members across all networks"></zt-navbar>
src/pages/controllers.ts:75:                <zt-navbar title="Controllers" subtitle="Controller and peer status"></zt-navbar>
src/pages/controllers.ts:83:            <zt-navbar title="Controllers" subtitle="Controller and peer status"></zt-navbar>
src/pages/dashboard.ts:241:                <zt-navbar title="Dashboard" subtitle="Overview"></zt-navbar>
src/pages/dashboard.ts:249:            <zt-navbar title="Dashboard" subtitle="Overview"></zt-navbar>
src/pages/networks.ts:137:            <zt-navbar title="Networks" subtitle="Manage your ZeroTier networks"></zt-navbar>
src/pages/network-detail.ts:665:                <zt-navbar title="Network Detail" subtitle=${this.networkId}></zt-navbar>
src/pages/network-detail.ts:680:            <zt-navbar title="Network Detail" subtitle=${this.networkId}></zt-navbar>
```

The 10 dead `import '../components/navbar.js';` lines to delete:
```
src/pages/settings.ts:6
src/pages/members.ts:9
src/pages/logs.ts:8
src/pages/users.ts:10
src/pages/networks.ts:9
src/pages/api-explorer.ts:6
src/pages/pending.ts:9
src/pages/controllers.ts:8
src/pages/dashboard.ts:10
src/pages/network-detail.ts:12
```

users.test.ts the test block to delete (line numbers AFTER Plan 20-01 inserted the D-03 test — Plan 20-01 inserts at the closing `});` of describe('page-users UI'), so the navbar test block may now sit at lines 193-199 unchanged OR 193-199 with the D-03 test below it; the literal CONTENT to match is what matters):
```ts
    it('renders navbar with User Management title', async () => {
        const el = await createUsersPage();

        const navbar = el.shadowRoot!.querySelector('zt-navbar');
        expect(navbar).toBeTruthy();
        expect(navbar?.getAttribute('title') || (navbar as any)?.title).toContain('User');
    });
```

src/styles/theme.ts:67 — the line to delete:
```
        --navbar-height: 56px;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete <zt-navbar> markup + navbar imports from all 10 page files (D-13, D-16)</name>
  <files>src/pages/dashboard.ts, src/pages/networks.ts, src/pages/network-detail.ts, src/pages/members.ts, src/pages/controllers.ts, src/pages/settings.ts, src/pages/logs.ts, src/pages/api-explorer.ts, src/pages/pending.ts, src/pages/users.ts</files>
  <read_first>
    - .planning/phases/20-shell-users-page-regression-fixes/20-CONTEXT.md (decisions D-13 delete every invocation, D-14 keep component file, D-16 treat double-renders uniformly)
    - .planning/phases/20-shell-users-page-regression-fixes/20-PATTERNS.md (Pattern D — exact grep output for all 13 invocations and 10 imports; Per-File Pattern Assignments § Page files (10 of them) — D-13 section at lines 502-557; analog example at lines 522-553)
    - src/pages/dashboard.ts (lines 1-15 to see the import block; lines 235-255 to see both navbar invocations — Pattern D's "Analog precedent" snippet at 20-PATTERNS.md:522-538 walks through the desired before/after)
    - For each of the other 9 page files, read the lines around the navbar import (lines 1-15) and around each `<zt-navbar>` invocation (±5 lines) to confirm the surrounding render structure
  </read_first>
  <action>
    For each of the 10 page files listed in `<files>`, perform TWO mechanical edits:

    **Edit A — delete the navbar import line** at the file:line listed in `<interfaces>` (10 deletions total):

    | File | Line | Content to delete |
    |------|------|-------------------|
    | src/pages/settings.ts | 6 | `import '../components/navbar.js';` |
    | src/pages/members.ts | 9 | `import '../components/navbar.js';` |
    | src/pages/logs.ts | 8 | `import '../components/navbar.js';` |
    | src/pages/users.ts | 10 | `import '../components/navbar.js';` |
    | src/pages/networks.ts | 9 | `import '../components/navbar.js';` |
    | src/pages/api-explorer.ts | 6 | `import '../components/navbar.js';` |
    | src/pages/pending.ts | 9 | `import '../components/navbar.js';` |
    | src/pages/controllers.ts | 8 | `import '../components/navbar.js';` |
    | src/pages/dashboard.ts | 10 | `import '../components/navbar.js';` |
    | src/pages/network-detail.ts | 12 | `import '../components/navbar.js';` |

    Delete the WHOLE line including the trailing newline. Surrounding imports stay in place.

    **Edit B — delete each `<zt-navbar ...></zt-navbar>` element** at the file:line listed in `<interfaces>` (13 deletions total).

    For each invocation, delete the WHOLE `<zt-navbar ...></zt-navbar>` element including the trailing newline. The surrounding render structure (page-header divs, `<div style="margin-top: var(--space-lg, 1.5rem);">` wrappers, modals, data-tables, etc.) is PRESERVED.

    Per D-16 ("treat double-render pages uniformly"): in `dashboard.ts`, `controllers.ts`, and `network-detail.ts` (which have two invocations each — one in a loading branch, one in a loaded branch), DELETE BOTH. There is no "preserve loading-state navbar variant" carve-out.

    Per 20-PATTERNS.md Pattern D analog (dashboard.ts:238-251 walked through):
    ```ts
    // BEFORE
    render() {
        if (this.loading) {
            return html`
                <zt-navbar title="Dashboard" subtitle="Overview"></zt-navbar>
                <div style="margin-top: var(--space-lg, 1.5rem);">
                    <zt-loading variant="skeleton-cards" count="4"></zt-loading>
                </div>
            `;
        }
        return html`
            <zt-navbar title="Dashboard" subtitle="Overview"></zt-navbar>
            <div style="margin-top: var(--space-lg, 1.5rem);">
    ```
    ```ts
    // AFTER
    render() {
        if (this.loading) {
            return html`
                <div style="margin-top: var(--space-lg, 1.5rem);">
                    <zt-loading variant="skeleton-cards" count="4"></zt-loading>
                </div>
            `;
        }
        return html`
            <div style="margin-top: var(--space-lg, 1.5rem);">
    ```

    Per Pattern D note: keep the `<div style="margin-top: var(--space-lg, 1.5rem);">` wrapper exactly — it positions content below the now-app-level navbar, preserving visual rhythm.

    For `src/pages/network-detail.ts` specifically: the two invocations at lines 665 and 680 use `subtitle=${this.networkId}` (dynamic). Plan 20-02 added a static `subtitle: 'Members and settings'` to the `networks/:id` route metadata, so the dynamic network name no longer surfaces in the top strip — per D-07, that's the intended behavior. If the network name needs to remain visible to the user, it stays where it already is: inside the page body (verified by reading the existing page-header section above line 665).

    Constraints across all 10 files:
    - Do NOT modify any other markup inside the render templates (page-headers, data-tables, modals, action rows, loading states).
    - Do NOT touch any service imports, type imports, lucide-static imports, or other component imports (`../components/data-table.js`, `../components/modal.js`, etc.).
    - Do NOT touch any `@state`, `@property`, or class-method code.
    - Do NOT modify the page's `static styles` block.
    - After the edits, each page should still type-check and render the same content minus the navbar.

    Order-independence: the 10 page-file edits do not interact. You may apply them in any order. Use the Edit tool for each file; one Edit call per file should suffice for the import deletion plus one or two Edit calls per file for the invocation deletion(s).
  </action>
  <verify>
    <automated>npm test -- src/pages/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE "<zt-navbar" src/pages/ | wc -l` returns 0 (no per-page navbar invocations remain anywhere in src/pages/)
    - `grep -rE "import '\.\./components/navbar\.js'" src/pages/ | wc -l` returns 0 (no dead imports remain)
    - `grep -rn "components/navbar" src/pages/ | grep -v test | wc -l` returns 0 (defensive: catches alternate import shapes too)
    - `ls src/pages/dashboard.ts src/pages/networks.ts src/pages/network-detail.ts src/pages/members.ts src/pages/controllers.ts src/pages/settings.ts src/pages/logs.ts src/pages/api-explorer.ts src/pages/pending.ts src/pages/users.ts` confirms all 10 files still exist (they were not accidentally deleted)
    - `grep -c "margin-top: var(--space-lg" src/pages/dashboard.ts` returns at least 2 (one in loading branch, one in loaded branch — wrappers preserved)
    - `npm test -- src/pages/` exits 0 (all page tests pass; the only one that touched navbar — users.test.ts:193-199 — is updated in Task 2 of this plan; some page tests may have asserted navbar via different paths than the grep target — see acceptance: the npm test exit code is the authoritative check)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>
    All 13 `<zt-navbar>` invocations and all 10 navbar imports are deleted from src/pages/*.ts. Surrounding markup and imports are preserved. Page tests under src/pages/ pass. TypeScript type-check passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete the navbar-assertion test block from users.test.ts (D-15)</name>
  <files>src/pages/users.test.ts</files>
  <read_first>
    - src/pages/users.test.ts (the file being modified — read the WHOLE file because Plan 20-01 has already inserted a new D-03 test at the bottom of `describe('page-users UI')`, so the navbar-assertion test block may sit BEFORE or ALONGSIDE the new D-03 test depending on Plan 20-01's insertion site — locate the navbar assertion by its content, not by line number)
    - .planning/phases/20-shell-users-page-regression-fixes/20-CONTEXT.md (decision D-15 — move navbar assertions to app.test.ts; verified by grep that users.test.ts:196 (pre-Plan-20-01) is the ONLY occurrence in pages/*.test.ts)
    - .planning/phases/20-shell-users-page-regression-fixes/20-PATTERNS.md (src/pages/users.test.ts § D-15 section at lines 560-593)
    - src/app.test.ts (post-Plan-20-03 state — confirm the replacement coverage exists in `describe('zt-app persistent navbar (LAYOUT-01)', ...)`)
    - .planning/phases/20-shell-users-page-regression-fixes/20-01-SUMMARY.md (to see where Plan 20-01 inserted the new D-03 test — before or after the navbar-assertion block)
  </read_first>
  <action>
    In `src/pages/users.test.ts`, locate the it() block that asserts the navbar with User Management title. Identify it by literal CONTENT match (not by line number — Plan 20-01 may have shifted line offsets):

    ```ts
        it('renders navbar with User Management title', async () => {
            const el = await createUsersPage();

            const navbar = el.shadowRoot!.querySelector('zt-navbar');
            expect(navbar).toBeTruthy();
            expect(navbar?.getAttribute('title') || (navbar as any)?.title).toContain('User');
        });
    ```

    DELETE this entire it() block (7 lines including the closing `});`). Use the Edit tool with the full literal block above as `old_string`.

    Plan 20-01's D-03 test (`it('Users-page action buttons render Lucide icons at 16x16 inside data-table shadow root (USERS-01 / D-03)', ...)`) must SURVIVE deletion — it is a different test block addressing USERS-01, not navbar visibility.

    After deletion, `describe('page-users UI')` should contain:
    - All original page-level tests (USER-02 UI, USER-01 UI add-user flow, validation, role badge, fetch, loading state, getCurrentUser)
    - The D-03 computed-style regression test added by Plan 20-01
    - NO navbar-related tests (those moved to `src/app.test.ts` in Plan 20-03 Task 3)

    Do NOT touch:
    - The `describe('page-users UI', ...)` block opening
    - The closing `});` of `describe('page-users UI')`
    - The `describe('Edit User modal (USER-01)', ...)` block — entirely outside this scope
    - The `createUsersPage` helper, mocks, fetchSpy, or any other test infrastructure
    - The D-03 test added by Plan 20-01 (it stays — that test addresses USERS-01, not navbar visibility)

    Verify after deletion: `grep -c "zt-navbar" src/pages/users.test.ts` returns 0. Verify across the whole pages test directory: `grep -rE "zt-navbar" src/pages/*.test.ts` returns no output (confirmed empty during planning grep).
  </action>
  <verify>
    <automated>npm test -- src/pages/users.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "zt-navbar" src/pages/users.test.ts` returns 0
    - `grep -c "renders navbar with User Management title" src/pages/users.test.ts` returns 0
    - `grep -c "navbar\?\.getAttribute" src/pages/users.test.ts` returns 0
    - `grep -rE "zt-navbar" src/pages/*.test.ts | wc -l` returns 0 (entire pages test directory clean)
    - `grep -c "USERS-01 / D-03" src/pages/users.test.ts` returns 1 (Plan 20-01's D-03 test survived and was NOT accidentally deleted)
    - `grep -c "describe('page-users UI'" src/pages/users.test.ts` returns 1 (describe block boundary intact)
    - `grep -c "describe('Edit User modal" src/pages/users.test.ts` returns 1 (the Edit User describe intact)
    - `npm test -- src/pages/users.test.ts` exits 0
    - `npx tsc --noEmit -p tsconfig.json` exits 0
  </acceptance_criteria>
  <done>
    The `it('renders navbar with User Management title', ...)` block is deleted. The describe('page-users UI') block remains intact with its existing tests + the D-03 test from Plan 20-01. Equivalent coverage now lives in app.test.ts from Plan 20-03 Task 3.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Delete the dead --navbar-height token from theme.ts (UI-SPEC cleanup)</name>
  <files>src/styles/theme.ts</files>
  <read_first>
    - src/styles/theme.ts (lines 60-72 to see the surrounding token block; line 67 is the deletion target)
    - .planning/phases/20-shell-users-page-regression-fixes/20-UI-SPEC.md (§ Navbar height token cleanup — "deletion preferred")
    - .planning/phases/20-shell-users-page-regression-fixes/20-PATTERNS.md (src/styles/theme.ts § UI-SPEC § Navbar height token cleanup section at lines 485-498)
    - src/components/navbar.ts (post-Plan-20-03 state — verify `var(--navbar-height)` is no longer referenced; the consumer was removed in Plan 20-03 Task 1)
  </read_first>
  <action>
    In `src/styles/theme.ts`, locate line 67:
    ```
            --navbar-height: 56px;
    ```

    DELETE this single line (and only this line — including the trailing newline). The surrounding token block (lines 65-66 `--sidebar-width: 240px;` and `--sidebar-width-collapsed: 64px;`, line 69 `--transition-fast: 150ms ease;`) is UNTOUCHED. Both `--sidebar-width-*` tokens are still consumed (`sidebar.ts:20`, `app.ts:33`).

    Pattern preference (per UI-SPEC): outright deletion is preferred over leaving the token dormant. Phase 20 ships zero `var(--navbar-height)` references after Plan 20-03 Task 1 closes; the token is unambiguously dead.

    Sanity-check before deletion: confirm `grep -rn "var(--navbar-height)" src/` returns zero matches after Plan 20-03 Task 1 ran. If Plan 20-03 left a stray consumer, surface it (it shouldn't — Plan 20-03 Task 1's acceptance criterion explicitly verifies this).

    Do NOT touch:
    - Any other token in theme.ts (colors, spacing scale, radii, transitions, font sizes, font-mono, sidebar widths)
    - The `lightTheme` block starting near line 74
    - The `resetStyles` block (if present in this file)
    - The MIRROR-fenced literal block (mentioned in PROJECT.md key decisions — Phase 20 does not introduce hex literals, so this block stays untouched)
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "navbar-height" src/styles/theme.ts` returns 0
    - `grep -rE "var\(--navbar-height\)" src/ | wc -l` returns 0 (no consumers anywhere)
    - `grep -c "sidebar-width" src/styles/theme.ts` returns at least 2 (`--sidebar-width` and `--sidebar-width-collapsed` both still present — adjacent tokens were not accidentally deleted)
    - `grep -nE "^        --transition-fast:" src/styles/theme.ts` returns 1 match (token block boundary intact below the deleted line)
    - `grep -nE "^        --color-overlay:" src/styles/theme.ts` returns 1 match (token block boundary intact above the deleted line)
    - `npm test` exits 0
    - `npm run audit:i18n` exits 0 (no new strings introduced; Phase 19 audit script confirms no PT regression)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
    - `npm run build` succeeds (the build does not break from the token removal — there are no implicit consumers)
  </acceptance_criteria>
  <done>
    src/styles/theme.ts line 67 (--navbar-height: 56px;) is deleted. Adjacent tokens (--sidebar-width, --sidebar-width-collapsed, --transition-fast) are intact. No source file references `var(--navbar-height)` anywhere. Build, test suite, and i18n audit all green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan deletes per-page navbar markup, a stale test assertion, and a dead CSS token. No new code paths, no new endpoints, no new auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-08 | D (DoS — render lifecycle) | src/pages/* | accept | Removing per-page <zt-navbar> renders REDUCES total navbar lifecycle frequency from N-per-route-change to 1-for-app-lifetime, which is the desired LAYOUT-01 win. Strictly improves DoS posture (fewer setInterval setups, fewer health-check restarts). |
| T-20-09 | I (Information disclosure) | src/styles/theme.ts | accept | Deleting an unused CSS token cannot disclose information. |
| T-20-10 | T (Tampering — test coverage gap) | src/pages/users.test.ts | accept | The deleted assertion has equivalent coverage in src/app.test.ts (added by Plan 20-03 Task 3) — strictly better coverage because app-level test catches the gate even if a future page somehow re-introduces a per-page <zt-navbar>. |

**Phase character:** no new attack surface — deletion-only cleanup with equivalent or improved coverage.
</threat_model>

<verification>
1. `git diff --stat` shows 12 files modified (10 pages + users.test.ts + theme.ts), all deletions (no additions in this plan)
2. `npm test` exits 0 — full suite green
3. `npx tsc --noEmit -p tsconfig.json` exits 0
4. `npm run build` exits 0 — production build still succeeds
5. `npm run audit:i18n` exits 0 — Phase 19 closure preserved
6. Authoritative grep sweep: `grep -rE "<zt-navbar|import '\.\./components/navbar\.js'|var\(--navbar-height\)" src/ | grep -v "src/components/navbar" | grep -v "src/app" | wc -l` returns 0 (only the kept references in src/components/navbar.ts and src/app.ts remain)
7. Manual dev-mode smoke (deferrable to UAT): navigate `/dashboard` → `/networks` → `/users` → `/login` → `/setup` — navbar visible on the first three, hidden on the last two, header band aligned with `.brand` row throughout
</verification>

<success_criteria>
- LAYOUT-01 fully shipped: 13 per-page navbar invocations deleted; 10 dead imports deleted; navbar lives only in `<zt-app>`
- D-13 satisfied: every page file render is leaner
- D-15 satisfied: users.test.ts no longer asserts navbar (the app.test.ts coverage from Plan 20-03 replaces it)
- D-16 satisfied: double-render pages (dashboard, controllers, network-detail) treated uniformly — both branches lose their navbar
- UI-SPEC cleanup: `--navbar-height` token deleted
- Phase 19 closure preserved: `npm run audit:i18n` continues to pass
- Full test suite, TypeScript, and production build all green
- Phase 20 goal achieved: app shell (navbar + brand) renders correctly above re-rendering page outlet; navbar aligned with brand row; Users-page action buttons display Lucide icons (USERS-01 from Plan 20-01)
</success_criteria>

<output>
After completion, create `.planning/phases/20-shell-users-page-regression-fixes/20-04-SUMMARY.md`. Record:
- Total deletion count (expect 13 invocations + 10 imports + 1 test block + 1 CSS token = 25 deletions, ~25-30 net line removals)
- Confirmation that no additions occurred in this plan
- Confirmation that `src/components/navbar.ts` and `src/components/navbar.test.ts` are UNCHANGED (D-14 — component file kept)
- Confirmation that `grep -rE "<zt-navbar|var\(--navbar-height\)" src/pages src/styles` returns no output
- Any test that needed unexpected adjustment because a page test had ALSO referenced the navbar indirectly (none expected — grep verified during planning that only users.test.ts:196 referenced it)
- Final UAT-ready status: phase 20 ships USERS-01 + LAYOUT-01 + LAYOUT-02 + SC #1-#5
</output>
