---
phase: 19-internationalization-sweep
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - .planning/phases/19-internationalization-sweep/19-AUDIT.md
autonomous: false
requirements: [I18N-01, I18N-02]
requirements_addressed: [I18N-01, I18N-02]
tags: [i18n, audit, walkthrough, documentation]
user_setup: []
must_haves:
  goal: "The Phase 19 audit report exists at the locked path, embeds the verbatim Plan-19-01 script output, and has every row in the 69-row page-by-role walkthrough matrix signed off by the human reviewer after exercising the running dev server — closing I18N-01 and I18N-02."
  truths:
    - "`.planning/phases/19-internationalization-sweep/19-AUDIT.md` exists with the prescribed skeleton from 19-RESEARCH.md § 'Audit Report Template' (Title, Scope, Methodology, Automated Pass, Manual Pass, Findings, Sign-off sections)"
    - "The Automated-Pass section embeds the verbatim stdout of Plan-19-01's `npm run audit:i18n` run (Pass 1 + Pass 2 banners + the 'Audit clean' line) and records exit code 0"
    - "The Manual-Pass walkthrough matrix contains all 69 rows from 19-RESEARCH.md § 'Audit Report Template' (page × role × interaction), and every row's checkbox is checked (`[x]`) after the human reviewer exercised the live `npm run dev` server"
    - "The Findings section reads 'Clean.' (or, if D-08 fired in Plan 19-01, lists the surfaced strings + their English replacements + the commit shas)"
    - "The Sign-off section records the audit run date, walkthrough completion date, and signer name"
    - "Existing vitest suite (`cd src && npm test`) still passes — no regression"
  artifacts:
    - path: ".planning/phases/19-internationalization-sweep/19-AUDIT.md"
      provides: "The Phase 19 D-05.2 audit report — the second of three Phase 19 deliverables"
      contains: "# Phase 19: Internationalization Sweep — Audit Report"
  key_links:
    - from: ".planning/phases/19-internationalization-sweep/19-AUDIT.md § Automated Pass"
      to: ".planning/phases/19-internationalization-sweep/19-01-AUDIT-RUN.txt"
      via: "verbatim paste of Plan 19-01 captured run output"
      pattern: "Audit clean — no PT strings found in src/"
    - from: ".planning/phases/19-internationalization-sweep/19-AUDIT.md § Manual Pass"
      to: "running dev server at http://localhost:3001"
      via: "human reviewer exercises 69 page × role × interaction rows"
      pattern: "^- \\[x\\] |\\| \\[x\\] \\|"
  metrics:
    - "Audit report file exists: `test -f .planning/phases/19-internationalization-sweep/19-AUDIT.md` exits 0"
    - "Report has the prescribed top-level title: `head -1 .planning/phases/19-internationalization-sweep/19-AUDIT.md` equals `# Phase 19: Internationalization Sweep — Audit Report`"
    - "All seven prescribed sections present: `grep -cE '^## (Scope|Methodology|Automated Pass|Manual Pass|Findings|Sign-off)' .planning/phases/19-internationalization-sweep/19-AUDIT.md` returns >= 6 (Title + 6 H2 headings)"
    - "Embedded automated-pass output contains the 'Audit clean' line: `grep -q 'Audit clean — no PT strings found in src/' .planning/phases/19-internationalization-sweep/19-AUDIT.md` exits 0"
    - "Walkthrough matrix has 69 data rows: `grep -cE '^\\| (/[a-z]|\\(shell\\))' .planning/phases/19-internationalization-sweep/19-AUDIT.md` returns >= 69"
    - "Every walkthrough row is signed off (`[x]` not `[ ]`): `grep -cE '\\| \\[x\\] \\|' .planning/phases/19-internationalization-sweep/19-AUDIT.md` returns >= 69 AND `grep -cE '\\| \\[ \\] \\|' .planning/phases/19-internationalization-sweep/19-AUDIT.md` returns 0"
    - "All four role checkboxes ticked: `grep -cE '^- \\[x\\] (Admin|Operator|Viewer|Unauthenticated)' .planning/phases/19-internationalization-sweep/19-AUDIT.md` returns 4"
    - "Closing-requirements line present: `grep -q 'Closes Requirements: I18N-01, I18N-02' .planning/phases/19-internationalization-sweep/19-AUDIT.md` exits 0"
    - "Existing tests stay green: `cd src && npm test` exits 0"
---

<objective>
Write the Phase 19 audit report at `.planning/phases/19-internationalization-sweep/19-AUDIT.md` using the verbatim skeleton from `19-RESEARCH.md § "Audit Report Template"`, embed the captured `npm run audit:i18n` output from Plan 19-01, then perform the 69-row page-by-role manual walkthrough against the live `npm run dev` server and sign off each row.

Purpose: Delivers the third Phase 19 D-05 artifact (`19-AUDIT.md`). The audit report is the single artifact that closes I18N-01 and I18N-02 — the script in Plan 19-01 produces machine-checkable evidence, and this report produces the human-checked walkthrough evidence (D-04: toasts and 4xx error paths only render against a running server, so the manual pass is non-optional).

Output: `19-AUDIT.md` with all sections filled in, all 69 walkthrough rows signed `[x]`, Findings = "Clean." (expected per RESEARCH § "Independent re-verification"), Sign-off line filled in. Plan 19-01's `19-01-AUDIT-RUN.txt` working-file may be deleted or kept after consumption — both acceptable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-internationalization-sweep/19-CONTEXT.md
@.planning/phases/19-internationalization-sweep/19-RESEARCH.md
@.planning/phases/19-internationalization-sweep/19-PATTERNS.md
@.planning/phases/19-internationalization-sweep/19-VALIDATION.md
@.planning/phases/19-internationalization-sweep/19-01-AUDIT-RUN.txt
@.planning/phases/19-internationalization-sweep/19-01-PLAN.md
@.planning/phases/19-internationalization-sweep/19-01-SUMMARY.md
@.planning/phases/18-member-zt-client-version/18-VERIFICATION.md
</context>

<tasks>

<task type="auto" id="19-02-T1">
  <name>Task 1: Author 19-AUDIT.md from the prescribed skeleton + embed Plan-19-01 script output</name>
  <files>.planning/phases/19-internationalization-sweep/19-AUDIT.md</files>
  <read_first>
    - .planning/phases/19-internationalization-sweep/19-RESEARCH.md § "Audit Report Template" (lines ~411-564 — the canonical skeleton; copy verbatim then fill in the placeholders)
    - .planning/phases/19-internationalization-sweep/19-01-AUDIT-RUN.txt (the captured Plan 19-01 run output — paste verbatim into § "Automated Pass — Script Invocation")
    - .planning/phases/19-internationalization-sweep/19-CONTEXT.md § "Deliverable Shape" (D-05.2 — the report deliverable contract; D-05.3 — the closure note belongs in 19-VERIFICATION.md NOT here)
    - .planning/phases/19-internationalization-sweep/19-PATTERNS.md § ".planning/phases/19-internationalization-sweep/19-AUDIT.md" (the report's role-match analog at 18-VERIFICATION.md — for sign-off shape only — and the explicit "prefer heading-preamble over YAML frontmatter" rule)
    - .planning/phases/18-member-zt-client-version/18-VERIFICATION.md (the closest report-shape sibling — but DO NOT copy its YAML frontmatter; use the heading-preamble form per 19-PATTERNS.md)
  </read_first>
  <action>
Create `.planning/phases/19-internationalization-sweep/19-AUDIT.md` with the following content. The skeleton is copied verbatim from 19-RESEARCH.md § "Audit Report Template" (the canonical prescription per 19-PATTERNS.md). Replace bracketed placeholders with concrete values from Plan 19-01 and from this task's run.

**Concrete placeholder substitutions for THIS plan run:**
- `**Audit Date:**` → today's date in `YYYY-MM-DD` format (e.g. `2026-05-11` or whatever today is when the executor runs this).
- `**Auditor:**` → `Claude Code (gsd-executor)` — plus the human reviewer's name appended to the Sign-off section in Task 2.
- `**Outcome:**` → `Clean` (default — RESEARCH verifies zero PT strings expected). If Plan 19-01 surfaced any D-08 hits, this becomes `Findings — see § Findings` and the Findings table is filled in below.
- `**Closes Requirements:**` → `I18N-01, I18N-02` (exact text — required for the grep gate).
- The `{paste the actual output here...}` placeholder in § "Automated Pass — Script Invocation" → paste the **complete contents** of `.planning/phases/19-internationalization-sweep/19-01-AUDIT-RUN.txt` between the triple backticks. Do not edit or reformat the captured text.
- The `Exit code: {0|1}` placeholder → `Exit code: 0` (read from the last `exit=` line of 19-01-AUDIT-RUN.txt).
- The role checkboxes (`- [ ] Admin`, etc.) → leave `[ ]` at this task; they get ticked in Task 2 after the walkthrough.
- The walkthrough matrix rows (all 69) → leave the `[ ]` placeholders untouched at this task; they get ticked in Task 2.
- The "Findings" section → use the **"Clean."** paragraph form (expected outcome). If Plan 19-01 fired D-08, replace with the Findings table (one row per surfaced PT string, with the commit sha from Plan 19-01's fallback fix task) and remove the "Clean." paragraph.
- The "Sign-off" section dates → leave as `YYYY-MM-DD HH:MM` at this task; they get filled in Task 2.

**Verbatim skeleton (copy this into the file, then apply the substitutions above):**

```markdown
# Phase 19: Internationalization Sweep — Audit Report

**Audit Date:** YYYY-MM-DD
**Auditor:** Claude Code (gsd-executor)
**Outcome:** Clean
**Closes Requirements:** I18N-01, I18N-02

## Scope (per 19-CONTEXT.md D-01)

In scope:
- Lit templates (`html\`…\``) in `src/pages/*.ts` and `src/components/*.ts`
- Reactive `@property` / `@state` initial values that render as text
- Toast and log call-site message strings across `src/`
- Validation error strings in `src/server/auth/*.ts` and `src/server/routes/member-ip-validator.ts`
- Server response `error` / `message` / `details` fields in `src/server/routes/*.ts`
- Accessibility attributes: `aria-label`, `aria-description`, `title`, `placeholder`, `alt`

Out of scope (per 19-CONTEXT.md D-02):
- `console.log` / `console.warn` / `console.error` payloads
- `fastify.log.*` audit-log message strings
- Comments and JSDoc
- `*.test.ts` files
- `.planning/`, `.docs/`, `docs/`, `README.md`, `.TODO.md`

## Methodology (per 19-CONTEXT.md D-03 / D-04)

Two-pass audit:
1. Automated grep — `src/scripts/i18n-audit.sh` runs the locked accent-class regex and the locked PT token-list regex (44 tokens; seed list extended by `carregando`, `enviar`, `selecionar` per 19-RESEARCH.md § "PT Token List Audit").
2. Manual page-by-role walkthrough on the running app (`cd src && npm run dev`), exercising real toast and 4xx error paths.

## Automated Pass — Script Invocation

```bash
cd src
npm run audit:i18n
```

Output (verbatim):

```
{paste the entire contents of 19-01-AUDIT-RUN.txt here}
```

Exit code: `0`

## Manual Pass — Walkthrough Sign-off

Roles tested:
- [ ] Admin
- [ ] Operator
- [ ] Viewer
- [ ] Unauthenticated (login + setup screens only)

Each row below has been visually confirmed against the running app at `http://localhost:3001`.

| Page | Role | Interaction | English-only confirmed |
|------|------|-------------|-----------------------|
| /login | Unauthenticated | Render login form | [ ] |
| /login | Unauthenticated | Submit empty form (fires inline error) | [ ] |
| /login | Unauthenticated | Submit wrong password (fires toast / inline 401 error) | [ ] |
| /setup | Unauthenticated (first run) | Render step 1 (admin creation) | [ ] |
| /setup | Unauthenticated (first run) | Submit weak password (fires inline error) | [ ] |
| /setup | Unauthenticated (first run) | Render step 2 (ZT controller) | [ ] |
| /setup | Unauthenticated (first run) | Test connection (fires Connection Test modal) | [ ] |
| /dashboard | Admin | Render loaded dashboard (stats, recent networks, controller info) | [ ] |
| /dashboard | Admin | Render with empty controller (no networks state) | [ ] |
| /dashboard | Operator | Render loaded dashboard | [ ] |
| /dashboard | Viewer | Render loaded dashboard | [ ] |
| /networks | Admin | Render networks table | [ ] |
| /networks | Admin | Open "Add Network" modal, render form | [ ] |
| /networks | Admin | Submit network create — success toast | [ ] |
| /networks | Admin | Submit network create with duplicate name — error toast | [ ] |
| /networks | Operator | Render networks table (no Delete button visible) | [ ] |
| /networks | Viewer | Render networks table (no Create button visible) | [ ] |
| /networks/:id | Admin | Render Status, Members, Settings sections | [ ] |
| /networks/:id | Admin | Open Edit-Network modal | [ ] |
| /networks/:id | Admin | Submit Edit-Network — success toast | [ ] |
| /networks/:id | Admin | Open Delete-Network modal — confirm + cancel buttons | [ ] |
| /networks/:id | Admin | Member row: open IP chip-editor modal | [ ] |
| /networks/:id | Admin | IP chip-editor: type invalid IP — inline error fires | [ ] |
| /networks/:id | Admin | IP chip-editor: type out-of-route IP — 400 error toast (`'IP address is outside the network's managed routes'`) | [ ] |
| /networks/:id | Admin | IP chip-editor: type duplicate IP — 409 toast (`'IP address is already assigned to another member of this network'`) | [ ] |
| /networks/:id | Admin | Batch-authorize selected members — modal + success toast | [ ] |
| /networks/:id | Admin | Search members ("Showing N of M") | [ ] |
| /networks/:id | Admin | Empty filter state (no matches) | [ ] |
| /networks/:id | Operator | Render network detail (Delete-Network button hidden) | [ ] |
| /networks/:id | Viewer | Render network detail (Edit + Delete buttons hidden) | [ ] |
| /members | Admin | Render all-networks member roll-up | [ ] |
| /members | Operator | Render members roll-up | [ ] |
| /members | Viewer | Render members roll-up | [ ] |
| /pending | Admin | Render pending groups + empty state ("All clear") | [ ] |
| /pending | Operator | Render pending groups | [ ] |
| /pending | Viewer | Render pending groups | [ ] |
| /controllers | Admin | Render controller + peer status | [ ] |
| /controllers | Operator | Render controller + peer status | [ ] |
| /controllers | Viewer | Render controller + peer status | [ ] |
| /users | Admin | Render users table | [ ] |
| /users | Admin | Open "Add User" modal | [ ] |
| /users | Admin | Create user — success toast + credential modal | [ ] |
| /users | Admin | Create user with duplicate username — error toast (`'Username already exists'`) | [ ] |
| /users | Admin | Open "Edit User" modal | [ ] |
| /users | Admin | Edit user — rename to existing username — error toast | [ ] |
| /users | Admin | Open "Reset Password" modal | [ ] |
| /users | Admin | Open "Delete User" modal (cannot-delete-self path: confirm last-admin guard fires) | [ ] |
| /users | Operator | Route redirects to /dashboard | [ ] |
| /users | Viewer | Route redirects to /dashboard | [ ] |
| /settings | Admin | Render preferences (change-password form) | [ ] |
| /settings | Admin | Change password — success toast | [ ] |
| /settings | Admin | Change password with wrong current — error toast | [ ] |
| /settings | Operator | Render preferences | [ ] |
| /settings | Viewer | Render preferences | [ ] |
| /logs | Admin | Render in-memory log entries (varied levels) | [ ] |
| /logs | Admin | Empty state (no entries) | [ ] |
| /logs | Operator | Render log entries | [ ] |
| /logs | Viewer | Render log entries | [ ] |
| /api | Admin | Render API Explorer | [ ] |
| /api | Admin | Submit GET request — response panel | [ ] |
| /api | Admin | Submit DELETE — confirm-modal heading "Confirm DELETE Request" | [ ] |
| /api | Operator | Render API Explorer (operator-allowed) | [ ] |
| /api | Viewer | Route redirects to /dashboard | [ ] |
| (shell) | Any | Sidebar nav labels (Overview / Management / Tools / System sections; Dashboard / Networks / Members / Pending / User Management / API Explorer / Logs / Controllers / Preferences) | [ ] |
| (shell) | Any | Navbar status indicator ("Connected" / "Disconnected") | [ ] |
| (shell) | Any | Navbar log-out button (aria-label / title "Log out") | [ ] |
| (shell) | Any | Navbar theme-toggle button (aria-label / title "Switch to light/dark theme") | [ ] |
| (shell) | Any | Toast dismiss button (aria-label "Dismiss notification") | [ ] |
| (shell) | Any | Modal close button (aria-label "Close dialog") | [ ] |

## Findings

**Clean.** Both grep passes returned zero hits; every walkthrough row visually confirmed English. Phase 19 closes I18N-01 and I18N-02 with no source changes required.

## Sign-off

- Audit script run: YYYY-MM-DD HH:MM
- Walkthrough completed: YYYY-MM-DD HH:MM
- Signed off by: gsd-executor

---
*Audit performed per 19-CONTEXT.md D-03 / D-04. Re-run with `cd src && npm run audit:i18n` at any time.*
```

After writing the skeleton, perform the substitution: replace the `{paste the entire contents of 19-01-AUDIT-RUN.txt here}` line with the actual contents of `.planning/phases/19-internationalization-sweep/19-01-AUDIT-RUN.txt` (read it, paste it verbatim between the triple backticks). Replace `YYYY-MM-DD` in the **Audit Date:** preamble with today's actual date (use `date +%Y-%m-%d` to get it). Leave all other `YYYY-MM-DD HH:MM` placeholders (Sign-off section) for Task 2.

Hard constraints (DO NOT violate):
- The matrix has **exactly 69 data rows** (verified by RESEARCH § "Audit Report Template"). Do not collapse, omit, or invent rows. The "(shell) | Any | …" rows (6 rows at the bottom) count toward the 69.
- The Findings section uses the "Clean." prose form by default; only swap to the Findings table if Plan 19-01's `19-01-AUDIT-RUN.txt` shows `exit=1` and lists hits. The default assumption is `exit=0` per RESEARCH § "Independent re-verification".
- Do NOT add YAML frontmatter (per 19-PATTERNS.md § "Frontmatter pattern" — the heading-preamble form is correct for `19-AUDIT.md`; YAML frontmatter belongs in `19-VERIFICATION.md` written by the verifier at phase-close, NOT here).
- Do NOT write `19-VERIFICATION.md` in this task — that is the verifier's artifact (D-05.3) appended at `/gsd-verify-phase` time, not in plan-phase or execute-phase.

Why this matters: The audit report IS the deliverable that proves I18N-01 / I18N-02 are satisfied. The verbatim skeleton is the locked contract — every subsequent re-audit (e.g. a v3.2 phase repeating the sweep) re-uses this exact shape.
  </action>
  <verify>
    <automated>test -f .planning/phases/19-internationalization-sweep/19-AUDIT.md && head -1 .planning/phases/19-internationalization-sweep/19-AUDIT.md | grep -q "^# Phase 19: Internationalization Sweep — Audit Report$" && grep -q "Closes Requirements:.*I18N-01.*I18N-02" .planning/phases/19-internationalization-sweep/19-AUDIT.md && grep -q "Audit clean — no PT strings found in src/" .planning/phases/19-internationalization-sweep/19-AUDIT.md && grep -cE '^\| (/[a-z]|\(shell\))' .planning/phases/19-internationalization-sweep/19-AUDIT.md | awk '$1>=69{exit 0}{exit 1}' && grep -cE '^## (Scope|Methodology|Automated Pass|Manual Pass|Findings|Sign-off)' .planning/phases/19-internationalization-sweep/19-AUDIT.md | awk '$1>=6{exit 0}{exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/19-internationalization-sweep/19-AUDIT.md` exits 0
    - Top-level title exact: `head -1 .planning/phases/19-internationalization-sweep/19-AUDIT.md` returns `# Phase 19: Internationalization Sweep — Audit Report`
    - Closes-Requirements line present: `grep -q "Closes Requirements:.*I18N-01.*I18N-02" .planning/phases/19-internationalization-sweep/19-AUDIT.md` exits 0
    - All six H2 section headings present: `grep -cE '^## (Scope|Methodology|Automated Pass|Manual Pass|Findings|Sign-off)' .planning/phases/19-internationalization-sweep/19-AUDIT.md` >= 6
    - Script-output paste embedded: `grep -q "Audit clean — no PT strings found in src/" .planning/phases/19-internationalization-sweep/19-AUDIT.md` exits 0 (or, if D-08 fired in Plan 19-01, the embedded grep-hit lines from the captured run)
    - Exit code line present and = 0: `grep -q "^Exit code: \`0\`$" .planning/phases/19-internationalization-sweep/19-AUDIT.md` exits 0
    - Walkthrough matrix has 69 data rows: `grep -cE '^\| (/[a-z]|\(shell\))' .planning/phases/19-internationalization-sweep/19-AUDIT.md` >= 69
    - At this task only, walkthrough rows are still unsigned (`[ ]`): `grep -cE '\| \[ \] \|' .planning/phases/19-internationalization-sweep/19-AUDIT.md` >= 69 (will flip to 0 after Task 2 walkthrough)
    - Audit Date is concrete (not the YYYY-MM-DD placeholder): `grep -q "^\*\*Audit Date:\*\* YYYY-MM-DD$" .planning/phases/19-internationalization-sweep/19-AUDIT.md` exits **NON-ZERO** (placeholder replaced)
    - No YAML frontmatter at top of file: `head -1 .planning/phases/19-internationalization-sweep/19-AUDIT.md` does NOT equal `---`
    - No accidental write of 19-VERIFICATION.md: `test ! -f .planning/phases/19-internationalization-sweep/19-VERIFICATION.md` exits 0 (that file is for /gsd-verify-phase, not here)
  </acceptance_criteria>
  <done>
`19-AUDIT.md` exists at the locked path with the prescribed skeleton, all six H2 sections present, the verbatim Plan-19-01 script output embedded in § "Automated Pass", and 69 walkthrough rows in `[ ]` state (ready for Task 2 to sign off). Findings reads "Clean." (or Findings table if D-08 fired).
  </done>
</task>

<task type="checkpoint:human-verify" id="19-02-T2" gate="blocking">
  <name>Task 2: Run dev server, perform 69-row page-by-role walkthrough, tick every checkbox, sign Sign-off block</name>
  <files>.planning/phases/19-internationalization-sweep/19-AUDIT.md</files>
  <read_first>
    - .planning/phases/19-internationalization-sweep/19-AUDIT.md (the freshly-written skeleton from Task 1 — this is the working document)
    - .planning/phases/19-internationalization-sweep/19-RESEARCH.md § "Walkthrough Matrix (Reference)" (page → role reachability — confirms which roles each route renders for; lines ~570-600)
    - .planning/phases/19-internationalization-sweep/19-CONTEXT.md D-04 (walkthrough is performed against `npm run dev` — not a static dump — so 4xx code paths fire)
    - .planning/phases/19-internationalization-sweep/19-PATTERNS.md § "Convention: D-09 copy style" (what English copy SHOULD look like — reviewer uses this as their oracle)
  </read_first>
  <what-built>
Task 1 created `19-AUDIT.md` from the verbatim 19-RESEARCH.md skeleton, with the prescribed sections, the 69-row walkthrough matrix in unsigned `[ ]` state, the embedded Plan-19-01 script output (Pass 1 + Pass 2 banners + the "Audit clean" line + `Exit code: 0`), and the Findings section set to "Clean.".

The audit script (Plan 19-01) already produced machine-checkable evidence that zero PT strings remain in `src/` source files. This task adds the human-checked evidence: the audit-locked decision D-04 requires the walkthrough to be performed against a **live** running app because some user-visible text only renders when a code path fires (e.g. 4xx error toasts, modal heading interpolations, dynamic aria-labels).
  </what-built>
  <how-to-verify>
1. **Start the dev server** in a terminal:
   ```bash
   cd src && npm run dev
   ```
   This boots Fastify on port 3000 (server) and Vite on port 3001 (SPA dev server). Wait for both `concurrently` panes to print their ready banners (`Server listening at http://0.0.0.0:3000` and `Local: http://localhost:3001`).

2. **Open the SPA at `http://localhost:3001`** in a browser.

3. **Walk through each of the 69 rows** in `19-AUDIT.md § Manual Pass — Walkthrough Sign-off`. For every row:
   - Authenticate as the role listed in the "Role" column. (Three test users were created by prior milestones — confirm seed users exist; if not, use `/setup` to create an admin, then create operator + viewer accounts via `/users`.)
   - Perform the action listed in the "Interaction" column. For 4xx-firing rows (e.g. "Submit network create with duplicate name — error toast"), deliberately seed the trigger condition (create a network with name "test-dup", then try to re-create it; type a duplicate IP into the chip editor; etc.).
   - Visually confirm **every rendered string** is English — page heading, button labels, table headers, table cell text, modal heading, modal body, toast title, toast description, inline error, empty-state message, aria-label tooltip (hover the icon buttons), `<select>` placeholder, `<input>` placeholder.
   - If the row passes, change `| [ ] |` to `| [x] |` in `19-AUDIT.md`.
   - If you find a PT string, STOP. Do NOT tick the row. Note the file:line and the PT text in a working note, switch to the D-08 fallback in Plan 19-01 (instantiate the D-08 fix-task template, replace the string inline, re-run `npm run audit:i18n`, and only then come back to tick this row). Add a row to `19-AUDIT.md § Findings` table per the format documented in RESEARCH § "Audit Report Template" lines 549-555.

4. **After all 69 rows are ticked**, also tick the four role-summary checkboxes near the top of § "Manual Pass":
   - `- [x] Admin`
   - `- [x] Operator`
   - `- [x] Viewer`
   - `- [x] Unauthenticated (login + setup screens only)`

5. **Fill in the Sign-off block** at the bottom of `19-AUDIT.md`:
   - `Audit script run:` → the timestamp from `19-01-AUDIT-RUN.txt` (or today's date + time if you can't extract it precisely).
   - `Walkthrough completed:` → today's date + the time you finished the last row, in `YYYY-MM-DD HH:MM` format.
   - `Signed off by:` → `gsd-executor (walkthrough by {human reviewer name})` — replace `{human reviewer name}` with the actual reviewer.

6. **Confirm no test regression**: `cd src && npm test` — must exit 0.

**Expected outcome (per RESEARCH § "Independent re-verification"):** All 69 rows tick cleanly; Findings stays "Clean."; the audit closes I18N-01 + I18N-02 with no source changes required. If the walkthrough surfaces ANY PT string the static audit missed, that's a real D-08 fix scenario — handle it per step 3 above and re-run the audit script before signing the row.

**Time estimate for the walkthrough:** ~30-45 minutes for an experienced reviewer. The 69 rows are dense but most are single-page-render checks; only ~15 rows require deliberate 4xx triggers (duplicate username, out-of-route IP, etc.).
  </how-to-verify>
  <resume-signal>Type "approved" once every row is `[x]`, all four role summary checkboxes are `[x]`, the Sign-off block is filled in with concrete dates and a reviewer name, and `cd src && npm test` exits 0. If the walkthrough surfaced PT strings, describe each one (file:line + the PT text + the proposed D-09 English replacement) and DO NOT type "approved" until the D-08 fallback fix has been applied (in Plan 19-01) AND the audit re-runs clean AND every walkthrough row that exposed the string is re-checked.</resume-signal>
  <verify>
    <automated>grep -cE '\| \[x\] \|' .planning/phases/19-internationalization-sweep/19-AUDIT.md | awk '$1>=69{exit 0}{exit 1}' && grep -cE '\| \[ \] \|' .planning/phases/19-internationalization-sweep/19-AUDIT.md | awk '$1==0{exit 0}{exit 1}' && grep -cE '^- \[x\] (Admin|Operator|Viewer|Unauthenticated)' .planning/phases/19-internationalization-sweep/19-AUDIT.md | awk '$1>=4{exit 0}{exit 1}' && ! grep -q "YYYY-MM-DD HH:MM" .planning/phases/19-internationalization-sweep/19-AUDIT.md && cd src && npm test</automated>
  </verify>
  <acceptance_criteria>
    - All 69 walkthrough rows are signed: `grep -cE '\| \[x\] \|' .planning/phases/19-internationalization-sweep/19-AUDIT.md` >= 69
    - Zero unsigned walkthrough rows remain: `grep -cE '\| \[ \] \|' .planning/phases/19-internationalization-sweep/19-AUDIT.md` returns 0
    - All four role-summary checkboxes ticked: `grep -cE '^- \[x\] (Admin|Operator|Viewer|Unauthenticated)' .planning/phases/19-internationalization-sweep/19-AUDIT.md` >= 4
    - Sign-off block has concrete dates (no `YYYY-MM-DD HH:MM` placeholder remains): `grep -c "YYYY-MM-DD HH:MM" .planning/phases/19-internationalization-sweep/19-AUDIT.md` returns 0
    - "Audit script run:" line has a real date: `grep -E "^- Audit script run: [0-9]{4}-[0-9]{2}-[0-9]{2}" .planning/phases/19-internationalization-sweep/19-AUDIT.md` exits 0
    - "Walkthrough completed:" line has a real date: `grep -E "^- Walkthrough completed: [0-9]{4}-[0-9]{2}-[0-9]{2}" .planning/phases/19-internationalization-sweep/19-AUDIT.md` exits 0
    - "Signed off by:" line is filled in (not empty / not still "{name}"): `grep -E "^- Signed off by: .+" .planning/phases/19-internationalization-sweep/19-AUDIT.md` exits 0
    - Findings section reads "Clean." (default outcome): `grep -q "^\*\*Clean.\*\* Both grep passes returned zero hits" .planning/phases/19-internationalization-sweep/19-AUDIT.md` exits 0 — OR, if D-08 fired during walkthrough, the Findings section has a populated table with at least one row matching `^\| [0-9]+ \| .+\.ts:[0-9]+ \|`
    - Existing vitest suite still green: `cd src && npm test` exits 0
    - If any PT string was found during walkthrough, the audit script now also exits 0 (re-run after fix): `cd src && npm run audit:i18n; test $? -eq 0` exits 0
  </acceptance_criteria>
  <done>
Every row in the 69-row walkthrough matrix is `[x]`, all four role-summary checkboxes are `[x]`, the Sign-off block has concrete dates + reviewer name, Findings is either "Clean." (expected) or a populated table with D-08-applied commit shas, and the existing vitest suite is still green. Phase 19 audit is signed off; I18N-01 and I18N-02 are satisfied.
  </done>
</task>

</tasks>

<verification>
After both tasks, the executor confirms:

1. `test -f .planning/phases/19-internationalization-sweep/19-AUDIT.md` exits 0 (report exists at locked path).
2. `head -1 .planning/phases/19-internationalization-sweep/19-AUDIT.md` returns `# Phase 19: Internationalization Sweep — Audit Report` (title matches RESEARCH skeleton).
3. `grep -q "Closes Requirements:.*I18N-01.*I18N-02" .planning/phases/19-internationalization-sweep/19-AUDIT.md` exits 0 (requirements declared closed).
4. `grep -cE '\| \[x\] \|' .planning/phases/19-internationalization-sweep/19-AUDIT.md` returns >= 69 (every walkthrough row signed).
5. `grep -cE '\| \[ \] \|' .planning/phases/19-internationalization-sweep/19-AUDIT.md` returns 0 (no unsigned rows).
6. `grep -c "YYYY-MM-DD" .planning/phases/19-internationalization-sweep/19-AUDIT.md` returns 0 (no placeholder dates left).
7. `grep -q "Audit clean — no PT strings found in src/" .planning/phases/19-internationalization-sweep/19-AUDIT.md` exits 0 (script-output paste preserved verbatim — expected default outcome).
8. `cd src && npm test` exits 0 (no regression to existing test suite).
9. `cd src && npm run audit:i18n; test $? -eq 0` exits 0 (final audit-script state is clean — guards against the case where a D-08 fix landed mid-walkthrough).

Commits expected (per CLAUDE.md Conventional Commits):
- `docs(19): write audit report skeleton and embed audit run output` — adds `19-AUDIT.md` with the structural skeleton + verbatim Plan-19-01 output (Task 1).
- `docs(19): sign off i18n walkthrough — phase 19 audit complete` — flips all 69 row checkboxes + fills the Sign-off block (Task 2).
- (If D-08 fired in walkthrough) one or more `fix(<area>): replace PT string '<source>' with '<replacement>'` commits in the surface where the string lived. Plan 19-01's D-08 fix-task template documents the shape.

Working artifact cleanup: `.planning/phases/19-internationalization-sweep/19-01-AUDIT-RUN.txt` MAY be deleted after this plan consumes it (no longer needed once the verbatim text is embedded in `19-AUDIT.md`) OR retained as a working note. Either is acceptable; if kept, do not commit it.
</verification>

<success_criteria>
- `.planning/phases/19-internationalization-sweep/19-AUDIT.md` exists with the prescribed skeleton from 19-RESEARCH.md § "Audit Report Template": Title (`# Phase 19: Internationalization Sweep — Audit Report`), heading-preamble (`Audit Date`, `Auditor`, `Outcome`, `Closes Requirements`), and the six H2 sections (`Scope`, `Methodology`, `Automated Pass — Script Invocation`, `Manual Pass — Walkthrough Sign-off`, `Findings`, `Sign-off`).
- The Automated-Pass section embeds the verbatim Plan-19-01 captured run output (Pass 1 banner + Pass 2 banner + "Audit clean" line + `Exit code: 0`).
- The Manual-Pass walkthrough matrix has all 69 rows and every row is signed `[x]` after the human reviewer exercised the row's interaction against `cd src && npm run dev`.
- The four role-summary checkboxes (Admin, Operator, Viewer, Unauthenticated) are all `[x]`.
- The Findings section reads "Clean." (RESEARCH-verified expected outcome — scout found zero PT strings) OR, if D-08 fired during walkthrough, has a populated table with each PT string, its English replacement per D-09, and the commit sha that applied the fix.
- The Sign-off block has concrete dates (no `YYYY-MM-DD HH:MM` placeholders) and a real reviewer name.
- D-05.3 NOT violated — `19-VERIFICATION.md` was NOT written by this plan (that artifact is the verifier's at `/gsd-verify-phase` time).
- D-06 NOT violated — no new install/CI hook added to `src/package.json` (D-06 was already validated by Plan 19-01; this plan does not touch package.json).
- D-07 NOT violated — no new `*.test.ts` file created.
- Existing vitest suite (`cd src && npm test`) is green (SC #4).
- Audit script re-runs clean (`cd src && npm run audit:i18n` exits 0) — guards against the case where a D-08 fix landed mid-walkthrough and the executor forgot to re-run.
- Both I18N-01 (every visible UI string is English — proven by 69-row walkthrough + script Pass 1+2) and I18N-02 (server response messages are English — proven by walkthrough rows covering 4xx code paths: duplicate username, out-of-route IP, weak password, etc.) are closed.
</success_criteria>

<output>
After completion, create `.planning/phases/19-internationalization-sweep/19-02-SUMMARY.md` per the standard summary template, recording:
- Files created: `.planning/phases/19-internationalization-sweep/19-AUDIT.md`
- Final state of Findings (Clean. — OR — N items fixed with commit shas)
- Walkthrough duration (start → end times from the Sign-off block)
- Any D-08 fix tasks instantiated during walkthrough (file:line, PT string, English replacement, commit sha) — expected: none
- Final audit script state: exit 0
- Final vitest suite state: green
- Commits produced (sha + message)
- Hand-off note: Phase 19 is ready for `/gsd-verify-phase 19` to write the closure note in `19-VERIFICATION.md` (D-05.3).
</output>
